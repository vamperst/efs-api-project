# Copie para .env (gitignored) e preencha.
# Usado por scripts shell/Python locais (deploy, build, report).

# AWS
AWS_REGION=us-east-1
AWS_PROFILE=default

# Projeto (bate com var.project nas stacks Terraform)
PROJECT=efs-api-lab
ENV=dev

# Overrides opcionais
# API_IMAGE_TAG=v1
# TARGET_DATA_SIZE_GB=100
